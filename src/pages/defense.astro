---
import BaseLayout from '../layouts/BaseLayout.astro';

const programs = [
  {
    name: 'DroneBane',
    subtitle: 'Counter-UAS Interceptor Platform',
    description: 'Armed intercept platform for small UAS threats. Acoustic target localization using RPM-indexed self-noise cancellation, multi-agent deep RL engagement guidance, direct MAVLink C2 from mobile tactical platforms, IFF re-verification at instant of discharge, and continuous engagement authorization with pursue-without-engagement fallback.',
    patents: 8,
    patentNums: '001, 006, 007, 008, 009, 011, 014, 017',
    trl: 'TRL 4',
    tags: ['Autonomous Intercept', 'TAK-Native', 'MAVLink C2', 'Deep RL', 'Acoustic'],
    href: 'https://jeranaias.github.io/dronebane/',
    featured: true,
  },
  {
    name: 'ThermalHawk',
    subtitle: 'Thermal Drone Detection',
    description: 'Sub-2-million-parameter backbone for small object detection across thermal, EO, and SAR domains. Reparameterizable architecture with periodic optimizer reinitialization for small-dataset training. Cross-architecture knowledge distillation for edge deployment.',
    patents: 1,
    patentNums: '024 (drafted)',
    trl: 'TRL 4',
    tags: ['Computer Vision', 'Thermal IR', 'Edge Inference', 'Multi-Domain'],
    featured: false,
  },
  {
    name: 'CAIRN',
    subtitle: 'CBRN Sensor Fusion',
    description: 'Real-time contamination zone estimation from heterogeneous CBRN sensor networks. Spatial grid-accelerated fusion with per-sensor anomaly detection. ISA 2.0 canonical data model with embedded IDLH tables and CAIRN-extended Cursor-on-Target XML namespace.',
    patents: 2,
    patentNums: '015, 016',
    trl: 'Phase I Proposal',
    tags: ['Sensor Fusion', 'Chemical Defense', 'TAK Integration', 'ISA 2.0'],
    href: 'https://jeranaias.github.io/CAIRN/',
    featured: false,
  },
  {
    name: 'Aurora',
    subtitle: 'Acoustic Swarm Communications',
    description: 'Covert data transmission via engineered rotor acoustics. GPS-denied cooperative positioning through propeller-modulated acoustic ranging. Sub-decimeter accuracy, zero RF emissions.',
    patents: 1,
    patentNums: '014',
    trl: 'TRL 3',
    tags: ['GPS-Denied', 'Acoustic Comms', 'RF-Silent', 'Swarm'],
    href: 'https://jeranaias.github.io/aurora/',
    featured: false,
  },
  {
    name: 'Mutawazin',
    subtitle: 'Arabic NLP',
    description: 'Post-processing systems for Arabic text diacritization. Contrastive dual-head neural corrector with adaptive multi-zone confidence thresholding. Hierarchical triple-key error-report disambiguation. 62x smaller than SOTA at comparable DER.',
    patents: 2,
    patentNums: '012, 013',
    trl: 'Research',
    tags: ['Diacritization', 'Neural Correction', 'Arabic', 'Low-Resource'],
    featured: false,
  },
  {
    name: 'TRAVERSE',
    subtitle: 'S&T Decision Support',
    description: 'Technology reconnaissance and venture ecosystem research platform. Investment tracking, technology radar, MCDA scoring, SBIR pipeline analysis. 21 NIST controls implemented.',
    patents: 0,
    patentNums: '',
    trl: 'TRL 4',
    tags: ['Decision Support', 'SBIR Intel', 'SpaceWERX', 'MCDA'],
    href: 'https://jeranaias.github.io/TRAVERSE/',
    featured: false,
  },
  {
    name: 'Learned DCLAHE',
    subtitle: 'Neural Image Enhancement',
    description: 'Differentiable Contrast Limited Adaptive Histogram Equalization with backpropagation-optimized parameters. End-to-end trainable image enhancement module that plugs into any detection pipeline for degraded visual environments.',
    patents: 1,
    patentNums: '003',
    trl: 'Research',
    tags: ['Image Enhancement', 'Differentiable', 'End-to-End'],
    featured: false,
  },
];
---

<BaseLayout
  title="Defense | Thornveil"
  description="Counter-UAS autonomy, CBRN sensor fusion, thermal detection, acoustic communications. 15 defense patents filed across 6 programs."
>
  <section class="section-padding pt-32 md:pt-40">
    <div class="container-max mx-auto">
      <p class="text-[11px] font-semibold tracking-[0.3em] uppercase reveal" style="color: var(--color-accent);">Defense</p>
      <h1 class="text-fluid-3xl font-semibold tracking-tight mt-3 reveal reveal-delay-1">Defense technology</h1>
      <p class="text-base mt-4 max-w-2xl leading-relaxed reveal reveal-delay-2" style="color: var(--color-text-secondary);">
        Counter-UAS interceptors, CBRN sensor fusion, thermal detection, acoustic communications, and Arabic NLP. 15 defense-related patents filed across 6 programs. Concept to working demo in days, not years.
      </p>
    </div>
  </section>

  <section class="section-padding pt-12" style="background: var(--color-surface-secondary);">
    <div class="container-max mx-auto">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-5">
        {programs.map((p, i) => {
          const Tag = p.href ? 'a' : 'div';
          const linkProps = p.href ? { href: p.href, target: '_blank', rel: 'noopener noreferrer' } : {};
          return (
            <Tag
              {...linkProps}
              class={`reveal reveal-delay-${Math.min(i + 1, 4)} card-gradient-border p-7 group ${p.featured ? 'md:col-span-2' : ''} ${p.href ? 'cursor-pointer' : ''}`}
              data-tilt
            >
              <div class="relative z-10">
                <div class="flex items-baseline gap-3 mb-1 flex-wrap">
                  <h3 class="text-base font-semibold tracking-tight">{p.name}</h3>
                  <span class="text-xs" style="color: var(--color-text-tertiary);">{p.subtitle}</span>
                  {p.href && (
                    <span class="inline-flex items-center gap-1 font-mono text-[9px] tracking-wider uppercase px-2 py-0.5 rounded-full" style="background: var(--color-accent-muted); color: var(--color-accent);">
                      <svg class="w-2.5 h-2.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M13.5 6H5.25A2.25 2.25 0 003 8.25v10.5A2.25 2.25 0 005.25 21h10.5A2.25 2.25 0 0018 18.75V10.5m-10.5 6L21 3m0 0h-5.25M21 3v5.25" /></svg>
                      Demo
                    </span>
                  )}
                </div>
                <p class="text-sm leading-relaxed my-3" style="color: var(--color-text-secondary);">{p.description}</p>
                <div class="flex items-center justify-between flex-wrap gap-3">
                  <div class="flex flex-wrap gap-1.5">
                    {p.tags.map((t: string) => (
                      <span class="tag">{t}</span>
                    ))}
                  </div>
                  <div class="flex items-center gap-4 text-right">
                    {p.patents > 0 && (
                      <span class="inline-flex items-center gap-1 font-mono text-[9px] tracking-wider uppercase px-2 py-0.5 rounded-full" style="background: var(--patent-bg); color: var(--patent-text); border: 1px solid var(--patent-border);">
                        {p.patents} Patent{p.patents > 1 ? 's' : ''}
                      </span>
                    )}
                    <div>
                      <span class="font-mono text-sm font-bold" style="color: var(--color-accent);">{p.trl}</span>
                    </div>
                  </div>
                </div>
              </div>
            </Tag>
          );
        })}
      </div>
    </div>
  </section>

  <!-- WORK -->
  <section class="section-padding">
    <div class="container-max mx-auto max-w-3xl">
      <h2 class="text-fluid-xl font-semibold tracking-tight mb-8 reveal">Deployed work</h2>
      <div class="rounded-xl overflow-hidden reveal reveal-delay-1" style="border: 1px solid var(--color-border);">
        {[
          { label: 'AI Digital Staff Officer (Heywood)', detail: '8,400 lines Go + 4,400 lines TypeScript. FIPS 140-3, CAC/PKI, 4 database backends. Built for a Marine Corps training command.', href: 'https://heywood-tbs.nicefield-9a8db973.eastus.azurecontainerapps.io' },
          { label: 'Expert-Driven Development', detail: 'AI-assisted development training curriculum for Department of War personnel. Structured curricula, documentation templates, and modern dev practices.', href: 'https://jeranaias.github.io/ExpertDrivenDevelopment/' },
        ].map((w) => (
          <a href={w.href} target="_blank" rel="noopener noreferrer" class="work-row block cursor-pointer">
            <div class="flex items-center gap-2">
              <div class="text-sm font-semibold">{w.label}</div>
              <svg class="w-3 h-3 flex-shrink-0" style="color: var(--color-accent);" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M13.5 6H5.25A2.25 2.25 0 003 8.25v10.5A2.25 2.25 0 005.25 21h10.5A2.25 2.25 0 0018 18.75V10.5m-10.5 6L21 3m0 0h-5.25M21 3v5.25" /></svg>
            </div>
            <div class="text-xs leading-relaxed mt-1" style="color: var(--color-text-tertiary);">{w.detail}</div>
          </a>
        ))}
      </div>
    </div>
  </section>
</BaseLayout>

<script>
  import Lenis from 'lenis';
  const lenis = new Lenis({ duration: 1.2, easing: (t: number) => Math.min(1, 1.001 - Math.pow(2, -10 * t)) });
  function raf(time: number) { lenis.raf(time); requestAnimationFrame(raf); }
  requestAnimationFrame(raf);
  document.querySelectorAll<HTMLElement>('[data-tilt]').forEach((card) => {
    const MAX_DEG = 3;
    card.addEventListener('mousemove', (e: MouseEvent) => { const r=card.getBoundingClientRect(); card.style.transform=`perspective(800px) rotateX(${(0.5-(e.clientY-r.top)/r.height)*MAX_DEG}deg) rotateY(${((e.clientX-r.left)/r.width-0.5)*MAX_DEG}deg)`; card.style.transition='transform 0.1s ease-out'; });
    card.addEventListener('mouseleave', () => { card.style.transform='perspective(800px) rotateX(0deg) rotateY(0deg)'; card.style.transition='transform 0.4s ease-out'; });
  });
</script>
