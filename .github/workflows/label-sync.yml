name: Label sync

on:
  schedule:
    - cron: '0 8 * * 1'
  workflow_dispatch:

jobs:
  sync:
    uses: thornveil-ai/.github/.github/workflows/label-sync.yml@main
    permissions:
      contents: read
      issues: write
